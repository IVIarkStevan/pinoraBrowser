<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3
    http://maven.apache.org/xsd/assembly-1.1.3.xsd">
    <id>dist</id>
    <formats>
        <format>zip</format>
        <format>dir</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>pinora-browser-${project.version}</baseDirectory>

    <files>
        <!-- Include the main project JAR -->
        <file>
            <source>${project.build.directory}/${project.build.finalName}.jar</source>
            <outputDirectory>.</outputDirectory>
        </file>
        <!-- Copy scripts -->
        <file>
            <source>${basedir}/pinora-browser.sh</source>
            <outputDirectory>.</outputDirectory>
            <fileMode>0755</fileMode>
        </file>
        <file>
            <source>${basedir}/pinora-browser.bat</source>
            <outputDirectory>.</outputDirectory>
        </file>
    </files>

    <!-- Include all dependencies in lib folder outside the JAR -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib</outputDirectory>
            <scope>runtime</scope>
            <useProjectArtifact>false</useProjectArtifact>
        </dependencySet>
    </dependencySets>
</assembly>

